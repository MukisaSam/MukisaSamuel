# Copy this file to ".env" and adjust the values if you need to.
# The real .env is listed in .gitignore and must NEVER be committed.

PROJECT_NAME=Student Information API

# Database. SQLite (default) needs no setup. For Postgres you'd use:
# DATABASE_URL=postgresql://user:password@localhost:5432/students
DATABASE_URL=sqlite:///./students.db
